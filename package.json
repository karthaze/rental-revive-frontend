{
  "name": "rentalrevive-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run render && vite",
    "dev:convex": "convex dev",
    "dev:probe": "node services/form-probe/index.mjs",
    "render": "node build/yards.mjs && node build/render.mjs",
    "build": "npm run render && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc -p services/convex",
    "yards": "node build/yards.mjs"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "@types/node": "^26.1.2",
    "@types/tz-lookup": "^6.1.2",
    "convex-test": "^0.0.54",
    "typescript": "^7.0.2",
    "vite": "^5.4.10",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "convex": "^1.42.3",
    "tz-lookup": "^6.1.25"
  }
}
